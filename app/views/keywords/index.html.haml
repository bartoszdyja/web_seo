%h2=website.name
%hr
.row
  .col-md-4
    =simple_form_for keyword, url: website_keywords_path(website) do |form|
      =form.input :name, label: 'Keyword'
      =form.button :submit, 'Add keywords'
%table.table.table-striped
  %thead
    %tr
      %th Keyword 
      %th Position
  %tbody
    -keywords.each do |k|
      %tr
        %td=k.name
        %td=k.website.name
